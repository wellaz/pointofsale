package com.equation.cashierll.receipts;

/**
 *
 * @author Wellington
 */

public class Header {
	public static String[] header = { "Items".toUpperCase(), "Qty".toUpperCase(), "Amount($)".toUpperCase() };

	public Header() {
		return;
	}
}